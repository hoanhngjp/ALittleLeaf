@section ChangeUserInfoSection{
    <link rel="stylesheet" href="~/css/changeUserInfo.css">
}
<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <div class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1 class="m-0">Thống kê</h1>
                </div><!-- /.col -->
                <div class="col-sm-6">
                    <ol class="breadcrumb float-sm-right">
                        <li class="breadcrumb-item"><a href="admin.html">Trang chủ</a></li>
                        <li class="breadcrumb-item active">Thống kê</li>
                    </ol>
                </div><!-- /.col -->
            </div><!-- /.row -->
        </div><!-- /.container-fluid -->
    </div>
    <!-- /.content-header -->
    <div class="table-footer" style="display: block;">
        <div class="timTheoNgay">
            Từ ngày: <input type="date" id="fromDate">
            Đến ngày: <input type="date" id="toDate">
            <button onclick="thongKe()"><i class="fa fa-search"></i>Thống kê</button>
        </div>
        <div style="visibility: hidden;">
            <td style="visibility: none;">Theo thu nhập</td>
            <select name="kieuTimDonHang" style="visibility: none;">
                <option value="ma">Từ cao đến thấp</option>
                <option value="khachhang">Từ thấp đến cao</option>
            </select>
            <button onclick="locDonHangTheoKhoangNgay()" style="visibility: none;"><i class="fa fa-search"></i> Tìm</button>
        </div>
    </div>
    <div class="wrap-table" id="result">
        <table id="userTable" class="table-header">
            <thead>
                <tr>
                    <th>Bill ID</th>
                    <th>Tên khách hàng</th>
                    <th>Ngày tạo đơn</th>
                    <th>Tổng giá trị đơn hàng</th>
                    <th>Số điện thoại khách hàng</th>
                    <th>Địa chỉ giao hàng</th>
                    <th>Phương thức thanh toán</th>
                    <th>Trạng thái thanh toán</th>
                    <th>Tình trạng đơn hàng</th>
                    <th>Trạng thái giao hàng</th>
                    <th>Ghi chú đơn hàng</th>
                    <th>Cập nhật đơn hàng</th>
                    <th>Thông tin đơn hàng</th>
                </tr>
            </thead>
            <tbody>
            <!--Mỗi Form bọc 1 tr-->
                <form action="./function/updateBill.php" method="POST">
                    <tr>
                        <td>' . $row['bill_id'] . '</td>
                        <input type="hidden" name="bill_id" value="' . $row['bill_id'] . '">
                        <td>' . $row['user_fullname'] . '</td>
                        <td>' . date("d/m/Y", strtotime($row["date_created"])) . '</td>
                        <td>' . number_format($row['total_amount'], 0, ',', '.') . '₫</td>
                        <td>' . $row['phone'] . '</td>
                        <td>' . $row['shipping_address'] . '</td>
                        <td>' . $row['checkout_method'] . '</td>
                        <td>
                            <select name="payment_status">
                                <option value="pending" selected>Đang chờ</option>';
                                <option value="paid">Đã thanh toán</option>';
                            </select>
                        </td>
                        <td>
                            <select name="is_confirmed">
                                <option value="1" selected>Đã xác nhận</option>
                                <option value="0">Chưa xác nhận</option>
                            </select>
                        </td>
                        <td>
                            <select name="shipping_status">
                                <option value="fulfilled" selected>Đã giao hàng</option>
                                <option value="not_fulfilled">Chưa giao hàng</option>
                            </select>
                        </td>
                        <td class="editable" 
                            <textarea name="note">' . $row['note'] . '</textarea>
                        </td>
                        <td>
                            <button class="sign-in-btn" type="submit" value="">Cập nhật</button>
                        </td>
                        <td>
                            <a href="chitiet.php?bill_id=' . $row['bill_id'] . '" style="color: black;" i class="fa fa-eye"></i> Chi tiết</a>
                        </td>
                    </tr>
                </form>
            </tbody>
        </table>
        <div class="pagination">
            <a href="?page=<?php echo ($page - 1); ?>" class="prev">&laquo; Trang trước</a>
            <a href="?page=<?php echo $i; ?>" class="<?php echo ($page == $i) ? 'active' : ''; ?>"><?php echo $i; ?></a>
            <span>...</span>
            <a href="?page=<?php echo ($page + 1); ?>" class="next">Trang tiếp theo &raquo;</a>
        </div>
    </div>
</div>
        <!-- ./wrapper -->
        <!-- jQuery -->
        <script src="~/plugins/jquery/jquery.min.js"></script>
        <!-- jQuery UI 1.11.4 -->
        <script src="~/plugins/jquery-ui/jquery-ui.min.js"></script>
        <!-- Resolve conflict in jQuery UI tooltip with Bootstrap tooltip -->
        <script>
            $.widget.bridge('uibutton', $.ui.button)
        </script>
        <!-- Bootstrap 4 -->
        <script src="~/plugins/bootstrap/js/bootstrap.bundle.min.js"></script>
        <!-- ChartJS -->
        <script src="~/plugins/chart.js/Chart.min.js"></script>
        <!-- Sparkline -->
        <script src="~/plugins/sparklines/sparkline.js"></script>
        <!-- JQVMap -->
        <script src="~/plugins/jqvmap/jquery.vmap.min.js"></script>
        <script src="~/plugins/jqvmap/maps/jquery.vmap.usa.js"></script>
        <!-- jQuery Knob Chart -->
        <script src="~/plugins/jquery-knob/jquery.knob.min.js"></script>
        <!-- daterangepicker -->
        <script src="~/plugins/moment/moment.min.js"></script>
        <script src="~/plugins/daterangepicker/daterangepicker.js"></script>
        <!-- Tempusdominus Bootstrap 4 -->
        <script src="~/plugins/tempusdominus-bootstrap-4/js/tempusdominus-bootstrap-4.min.js"></script>
        <!-- Summernote -->
        <script src="~/plugins/summernote/summernote-bs4.min.js"></script>
        <!-- overlayScrollbars -->
        <script src="~/plugins/overlayScrollbars/js/jquery.overlayScrollbars.min.js"></script>
        <!-- AdminLTE App -->
        <script src="~/dist/js/adminlte.js"></script>
        <!-- AdminLTE for demo purposes -->
        <script src="~/dist/js/demo.js"></script>
        <!-- AdminLTE dashboard demo (This is only for demo purposes) -->
        <script src="~/dist/js/pages/dashboard.js"></script>
        <script>
            function thongKe() {
                var fromDate = document.getElementById("fromDate").value;
                var toDate = document.getElementById("toDate").value;

                // Gửi dữ liệu ngày tháng lên máy chủ bằng Ajax
                $.ajax({
                    type: "POST",
                    url: "./function/thongke.php",
                    data: {
                        fromDate: fromDate,
                        toDate: toDate
                    },
                    success: function (response) {
                        // Hiển thị kết quả trả về từ máy chủ
                        $("#result").html(response);
                    }
                });
            }
        </script>
